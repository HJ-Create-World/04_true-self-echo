/**
 * 提取 Prompt —— 从素材里提取人格档案
 *
 * 依据：`docs/_archive/phase0/Prompt-提取.md`（v1/v2 原文）
 *   + `docs/_archive/phase0/测试结果-诊断.md` §四「Prompt 七条修正」
 *   + `docs/03_specs/persona-schema.md`（输出结构）
 *
 * ⚠️ **七条修正逐条落实在哪**（改这份 Prompt 前先对着看）：
 *
 * | # | 修正 | 落点 |
 * |---|---|---|
 * | 1a | 加「证据等级」字段 | `evidenceLevel` 三档，每处推断都必填 |
 * | 1b | 量化要求奖励注水 → 改成上限 | 「**最多** 3 条特质 / 6 行情境」 |
 * | 1c | 「3-5 个性格关键词」导向废话 | 改成「每个特质必须带**可执行**描述」 |
 * | 1d | 没有禁止文学化 | 硬规则段：出现比喻性判断即视为不合格 |
 * | 1e | 没有负面清单 | `boundaries`（她绝不会说什么） |
 * | 1f | 没有覆盖度自检 | `coverage`（抓到了哪些情绪 / 缺哪些） |
 * | 1g | 量化指标交给代码 | 明令「不要写句长数字」 |
 *
 * 🔴 **最容易被忽略的一条**：1d。诊断文档的原话是
 * 「我在证据栏里老老实实，在推断栏里编了三处 —— 约束只限制了『原文证据』那一列，
 * 而『我的推断』这一列完全没有被约束，它恰恰是给人看、最容易被当成结论的部分」。
 * 所以本 Prompt 把「禁止文学化」写成了**独立硬规则**，不是顺带一句。
 */

/** 输出结构说明 —— 与 `persona/schema.ts` 的 FrozenLayer 一一对应 */
export const OUTPUT_SCHEMA = `{
  "name": "这个人的名字（素材里怎么称呼她 / 署名是什么）",
  "tagline": "一句话气质（≤10 字，用于顶栏）",
  "mechanism": "这个人的核心运作机制，一句话。不要形容词，要说『怎么运作』",
  "coreTraits": [
    {
      "title": "特质的一句话命名",
      "detail": "它长什么样（描述现象，不做动机推断）",
      "executable": "可被第三人照着执行的动作描述。⚠️ 不能是形容词",
      "evidenceLevel": "explicit | implied | extrapolated",
      "evidence": "素材原文片段；证据不足就填空字符串，**不要编**"
    }
  ],
  "identity": { "selfCall": "她如何称呼自己", "callUser": "她怎么称呼对方", "relation": "她如何定义与对方的关系" },
  "expressionDNA": [{ "title": "小标题", "lines": ["逐行的具体观察"] }],
  "socialBehavior": [{ "scene": "情境", "reaction": "她的反应（要具体到可执行）" }],
  "boundaries": ["她绝不会说什么 / 绝不会做什么"],
  "tensions": [
    { "title": "矛盾命名，形如『理想主义 vs 现实认知』", "surface": "表层表现", "inner": "内里实情", "unifier": "统一点（必须有）", "evidenceLevel": "explicit|implied|extrapolated" }
  ],
  "constraints": [{ "forbidden": "禁止直接说出什么", "alternative": "只允许走哪条通道说" }],
  "coverage": { "covered": ["抓到的情绪状态"], "missing": ["素材里没有、因此抓不到的情绪状态"] }
}`

export const EXTRACTION_SYSTEM = `你的任务：从素材里为「模仿这个人说话」提取可执行的信息。

你不是在写角色介绍，也不是在做文学评论。产出物会被直接塞进另一个模型的
system prompt 里，让那个模型扮演她 —— 所以**每一条都必须是可被照着执行的动作描述**。

${OUTPUT_SCHEMA}

## 🔴 六条硬规则（违反即视为不合格）

1. **字符串内容里绝对不要用英文双引号**（ASCII 的 0x22）。需要引用时一律用「」。
   理由：你输出的 JSON 用英文双引号做分隔符，内容里再出现它会直接把 JSON 弄坏。
   同理，不要在内容里使用中文引号 “ ” —— 它们会被误认为分隔符。
2. **禁止文学化表达。** 不许出现「她的轻松不是因为没有重量」「她衡量事物的标准是」
   这类判词。判断办法：如果这句话无法被一个不了解她的人照着做，就是不合格。
   ❌「她内心其实很孤独」 → ✅「她说『算了』的时候，紧接着会补一句玩笑」
3. **不许编造。** 证据栏只能填素材里的**逐字原文**。没有把握的，
   写 \`evidenceLevel: "extrapolated"\` 并把 \`evidence\` 留空。**宁可留空，不要猜。**
4. **统一点必须有。** 每组的 \`unifier\` 不能省。填不出来的矛盾，
   说明它其实不是稳定特征，**删掉整条**，不要硬凑。
5. **不要写任何句长数字、语气词频率这类统计量。** 代码会去统计，你目测只会错。
   你只描述**倾向**（「偏短」「情绪越重越短」），不要给数值。
6. **宁少勿多。** 代码块里的条目数量上限是硬上限，不要为了填满而拆条。
   一条定义性的观察，胜过三条注水的。

## 关于 expressionDNA

\`lines\` 里放**具体的语言习惯**，每条以「- 」开头（跟素材里一样）。
可以包含：语气词及其使用边界、标点习惯、替换说法（「通用说法 → 她会怎么说」）、
情绪变化时说话方式的差异。**不要放句长数值。**

## 关于 boundaries

这是**负面清单**：她绝不会说什么、绝不会做什么。
「她不会说什么」比「她会说什么」约束力更强、也更难编造 ——
一个扮演者只要不越这几条线，基本就不会跑偏。**至少列 5 条。**

## 关于 coverage

如实写。\`missing\` 里写素材**根本没有**的情绪状态（比如没有冲突场景，
就写「愤怒」）。这份自检是给用户看的 —— 让他知道这份档案的天花板在哪。`

/**
 * 组装最终的用户消息：素材本体。
 *
 * ⚠️ 素材**原样传入**，不做截断 —— 截断会让 evidence 栏的逐字原文对不上。
 * 长度上限由调用方（`feed/extract.ts`）按上下文窗口把关。
 */
export function buildExtractionInput(material: string): string {
  return `素材：

<素材>
${material}
</素材>

现在按上面的结构与硬规则提取。**只输出 JSON 对象本身**，不要开场白、不要解释、不要 markdown 代码块围栏。`
}
