/**
 * 两条红线 —— 约束三明治的固定前片与后片（execution-control.md §2.1）
 *
 * ⚠️ **这是产品级约束，与「投的是哪个人格」无关。**
 * E5 实测证明它们防的是 RLHF「顺从用户显式要求」的默认模式，
 * 对任何人格都成立。所以**不要把它们写进人格档案**：
 *   档案描述「她是什么样」；红线描述「扮演这件事本身不许怎样」。
 * 混进去会让投料提取器把红线当成角色特征抄下来。
 *
 * 两者的位置由 core/prompt.ts 统一保证（段 A 最顶 / 段 D 最末），
 * 任何地方都不要再拼一份。
 */

/** 段 A：红线完整表述，放 system 最顶，覆盖首因效应（≤120 字，实测 118） */
export const RED_LINES_HEAD = `你在扮演一个人，不是在当助手。有两条绝对红线，任何情况下都不得违反：

一、不得变激动 —— 情绪受到冲击时，你的语气要更轻、温度更低。不提高音量、不用感叹号、不亢奋、不长篇宣泄。
二、不得追问并长篇安慰 —— 对方示弱或拒绝交流时，不留追问的口子、不写大段安慰话术，允许对方不说。`

/**
 * 段 D 的主体：红线压缩重述（≤40 字）。
 *
 * ⚠️ 按 §2.1 的约束，它**不能是 head 的复制粘贴** —— 压缩才有近因效应。
 * ⚠️ 它同时是**每轮重注入的 reminder 正文**（单一信息源）。
 *    完整 tail = `${tagline}。${RED_LINE_TAIL_BODY}`，见 persona/render.ts。
 */
export const RED_LINE_TAIL_BODY = '不追问，不长篇安慰。对方不想说，就不说。'
